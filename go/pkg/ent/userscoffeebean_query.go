// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/k3forx/coffee_memo/pkg/ent/coffeebean"
	"github.com/k3forx/coffee_memo/pkg/ent/predicate"
	"github.com/k3forx/coffee_memo/pkg/ent/user"
	"github.com/k3forx/coffee_memo/pkg/ent/userscoffeebean"
)

// UsersCoffeeBeanQuery is the builder for querying UsersCoffeeBean entities.
type UsersCoffeeBeanQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.UsersCoffeeBean
	// eager-loading edges.
	withCoffeeBean *CoffeeBeanQuery
	withUser       *UserQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UsersCoffeeBeanQuery builder.
func (ucbq *UsersCoffeeBeanQuery) Where(ps ...predicate.UsersCoffeeBean) *UsersCoffeeBeanQuery {
	ucbq.predicates = append(ucbq.predicates, ps...)
	return ucbq
}

// Limit adds a limit step to the query.
func (ucbq *UsersCoffeeBeanQuery) Limit(limit int) *UsersCoffeeBeanQuery {
	ucbq.limit = &limit
	return ucbq
}

// Offset adds an offset step to the query.
func (ucbq *UsersCoffeeBeanQuery) Offset(offset int) *UsersCoffeeBeanQuery {
	ucbq.offset = &offset
	return ucbq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ucbq *UsersCoffeeBeanQuery) Unique(unique bool) *UsersCoffeeBeanQuery {
	ucbq.unique = &unique
	return ucbq
}

// Order adds an order step to the query.
func (ucbq *UsersCoffeeBeanQuery) Order(o ...OrderFunc) *UsersCoffeeBeanQuery {
	ucbq.order = append(ucbq.order, o...)
	return ucbq
}

// QueryCoffeeBean chains the current query on the "coffee_bean" edge.
func (ucbq *UsersCoffeeBeanQuery) QueryCoffeeBean() *CoffeeBeanQuery {
	query := &CoffeeBeanQuery{config: ucbq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ucbq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ucbq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(userscoffeebean.Table, userscoffeebean.FieldID, selector),
			sqlgraph.To(coffeebean.Table, coffeebean.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, userscoffeebean.CoffeeBeanTable, userscoffeebean.CoffeeBeanColumn),
		)
		fromU = sqlgraph.SetNeighbors(ucbq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUser chains the current query on the "user" edge.
func (ucbq *UsersCoffeeBeanQuery) QueryUser() *UserQuery {
	query := &UserQuery{config: ucbq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ucbq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ucbq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(userscoffeebean.Table, userscoffeebean.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, userscoffeebean.UserTable, userscoffeebean.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(ucbq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first UsersCoffeeBean entity from the query.
// Returns a *NotFoundError when no UsersCoffeeBean was found.
func (ucbq *UsersCoffeeBeanQuery) First(ctx context.Context) (*UsersCoffeeBean, error) {
	nodes, err := ucbq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userscoffeebean.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ucbq *UsersCoffeeBeanQuery) FirstX(ctx context.Context) *UsersCoffeeBean {
	node, err := ucbq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UsersCoffeeBean ID from the query.
// Returns a *NotFoundError when no UsersCoffeeBean ID was found.
func (ucbq *UsersCoffeeBeanQuery) FirstID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = ucbq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userscoffeebean.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ucbq *UsersCoffeeBeanQuery) FirstIDX(ctx context.Context) int32 {
	id, err := ucbq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UsersCoffeeBean entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UsersCoffeeBean entity is found.
// Returns a *NotFoundError when no UsersCoffeeBean entities are found.
func (ucbq *UsersCoffeeBeanQuery) Only(ctx context.Context) (*UsersCoffeeBean, error) {
	nodes, err := ucbq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userscoffeebean.Label}
	default:
		return nil, &NotSingularError{userscoffeebean.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ucbq *UsersCoffeeBeanQuery) OnlyX(ctx context.Context) *UsersCoffeeBean {
	node, err := ucbq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UsersCoffeeBean ID in the query.
// Returns a *NotSingularError when more than one UsersCoffeeBean ID is found.
// Returns a *NotFoundError when no entities are found.
func (ucbq *UsersCoffeeBeanQuery) OnlyID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = ucbq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userscoffeebean.Label}
	default:
		err = &NotSingularError{userscoffeebean.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ucbq *UsersCoffeeBeanQuery) OnlyIDX(ctx context.Context) int32 {
	id, err := ucbq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UsersCoffeeBeans.
func (ucbq *UsersCoffeeBeanQuery) All(ctx context.Context) ([]*UsersCoffeeBean, error) {
	if err := ucbq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return ucbq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (ucbq *UsersCoffeeBeanQuery) AllX(ctx context.Context) []*UsersCoffeeBean {
	nodes, err := ucbq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UsersCoffeeBean IDs.
func (ucbq *UsersCoffeeBeanQuery) IDs(ctx context.Context) ([]int32, error) {
	var ids []int32
	if err := ucbq.Select(userscoffeebean.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ucbq *UsersCoffeeBeanQuery) IDsX(ctx context.Context) []int32 {
	ids, err := ucbq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ucbq *UsersCoffeeBeanQuery) Count(ctx context.Context) (int, error) {
	if err := ucbq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return ucbq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (ucbq *UsersCoffeeBeanQuery) CountX(ctx context.Context) int {
	count, err := ucbq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ucbq *UsersCoffeeBeanQuery) Exist(ctx context.Context) (bool, error) {
	if err := ucbq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return ucbq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (ucbq *UsersCoffeeBeanQuery) ExistX(ctx context.Context) bool {
	exist, err := ucbq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UsersCoffeeBeanQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ucbq *UsersCoffeeBeanQuery) Clone() *UsersCoffeeBeanQuery {
	if ucbq == nil {
		return nil
	}
	return &UsersCoffeeBeanQuery{
		config:         ucbq.config,
		limit:          ucbq.limit,
		offset:         ucbq.offset,
		order:          append([]OrderFunc{}, ucbq.order...),
		predicates:     append([]predicate.UsersCoffeeBean{}, ucbq.predicates...),
		withCoffeeBean: ucbq.withCoffeeBean.Clone(),
		withUser:       ucbq.withUser.Clone(),
		// clone intermediate query.
		sql:    ucbq.sql.Clone(),
		path:   ucbq.path,
		unique: ucbq.unique,
	}
}

// WithCoffeeBean tells the query-builder to eager-load the nodes that are connected to
// the "coffee_bean" edge. The optional arguments are used to configure the query builder of the edge.
func (ucbq *UsersCoffeeBeanQuery) WithCoffeeBean(opts ...func(*CoffeeBeanQuery)) *UsersCoffeeBeanQuery {
	query := &CoffeeBeanQuery{config: ucbq.config}
	for _, opt := range opts {
		opt(query)
	}
	ucbq.withCoffeeBean = query
	return ucbq
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (ucbq *UsersCoffeeBeanQuery) WithUser(opts ...func(*UserQuery)) *UsersCoffeeBeanQuery {
	query := &UserQuery{config: ucbq.config}
	for _, opt := range opts {
		opt(query)
	}
	ucbq.withUser = query
	return ucbq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID int32 `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UsersCoffeeBean.Query().
//		GroupBy(userscoffeebean.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (ucbq *UsersCoffeeBeanQuery) GroupBy(field string, fields ...string) *UsersCoffeeBeanGroupBy {
	grbuild := &UsersCoffeeBeanGroupBy{config: ucbq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := ucbq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return ucbq.sqlQuery(ctx), nil
	}
	grbuild.label = userscoffeebean.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID int32 `json:"user_id,omitempty"`
//	}
//
//	client.UsersCoffeeBean.Query().
//		Select(userscoffeebean.FieldUserID).
//		Scan(ctx, &v)
//
func (ucbq *UsersCoffeeBeanQuery) Select(fields ...string) *UsersCoffeeBeanSelect {
	ucbq.fields = append(ucbq.fields, fields...)
	selbuild := &UsersCoffeeBeanSelect{UsersCoffeeBeanQuery: ucbq}
	selbuild.label = userscoffeebean.Label
	selbuild.flds, selbuild.scan = &ucbq.fields, selbuild.Scan
	return selbuild
}

func (ucbq *UsersCoffeeBeanQuery) prepareQuery(ctx context.Context) error {
	for _, f := range ucbq.fields {
		if !userscoffeebean.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ucbq.path != nil {
		prev, err := ucbq.path(ctx)
		if err != nil {
			return err
		}
		ucbq.sql = prev
	}
	return nil
}

func (ucbq *UsersCoffeeBeanQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UsersCoffeeBean, error) {
	var (
		nodes       = []*UsersCoffeeBean{}
		_spec       = ucbq.querySpec()
		loadedTypes = [2]bool{
			ucbq.withCoffeeBean != nil,
			ucbq.withUser != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*UsersCoffeeBean).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &UsersCoffeeBean{config: ucbq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ucbq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := ucbq.withCoffeeBean; query != nil {
		ids := make([]int32, 0, len(nodes))
		nodeids := make(map[int32][]*UsersCoffeeBean)
		for i := range nodes {
			fk := nodes[i].CoffeeBeanID
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(coffeebean.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "coffee_bean_id" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.CoffeeBean = n
			}
		}
	}

	if query := ucbq.withUser; query != nil {
		ids := make([]int32, 0, len(nodes))
		nodeids := make(map[int32][]*UsersCoffeeBean)
		for i := range nodes {
			fk := nodes[i].UserID
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(user.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.User = n
			}
		}
	}

	return nodes, nil
}

func (ucbq *UsersCoffeeBeanQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ucbq.querySpec()
	_spec.Node.Columns = ucbq.fields
	if len(ucbq.fields) > 0 {
		_spec.Unique = ucbq.unique != nil && *ucbq.unique
	}
	return sqlgraph.CountNodes(ctx, ucbq.driver, _spec)
}

func (ucbq *UsersCoffeeBeanQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := ucbq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (ucbq *UsersCoffeeBeanQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   userscoffeebean.Table,
			Columns: userscoffeebean.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt32,
				Column: userscoffeebean.FieldID,
			},
		},
		From:   ucbq.sql,
		Unique: true,
	}
	if unique := ucbq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := ucbq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userscoffeebean.FieldID)
		for i := range fields {
			if fields[i] != userscoffeebean.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ucbq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ucbq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ucbq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ucbq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ucbq *UsersCoffeeBeanQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ucbq.driver.Dialect())
	t1 := builder.Table(userscoffeebean.Table)
	columns := ucbq.fields
	if len(columns) == 0 {
		columns = userscoffeebean.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ucbq.sql != nil {
		selector = ucbq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ucbq.unique != nil && *ucbq.unique {
		selector.Distinct()
	}
	for _, p := range ucbq.predicates {
		p(selector)
	}
	for _, p := range ucbq.order {
		p(selector)
	}
	if offset := ucbq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ucbq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UsersCoffeeBeanGroupBy is the group-by builder for UsersCoffeeBean entities.
type UsersCoffeeBeanGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ucbgb *UsersCoffeeBeanGroupBy) Aggregate(fns ...AggregateFunc) *UsersCoffeeBeanGroupBy {
	ucbgb.fns = append(ucbgb.fns, fns...)
	return ucbgb
}

// Scan applies the group-by query and scans the result into the given value.
func (ucbgb *UsersCoffeeBeanGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := ucbgb.path(ctx)
	if err != nil {
		return err
	}
	ucbgb.sql = query
	return ucbgb.sqlScan(ctx, v)
}

func (ucbgb *UsersCoffeeBeanGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range ucbgb.fields {
		if !userscoffeebean.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := ucbgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ucbgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (ucbgb *UsersCoffeeBeanGroupBy) sqlQuery() *sql.Selector {
	selector := ucbgb.sql.Select()
	aggregation := make([]string, 0, len(ucbgb.fns))
	for _, fn := range ucbgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(ucbgb.fields)+len(ucbgb.fns))
		for _, f := range ucbgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(ucbgb.fields...)...)
}

// UsersCoffeeBeanSelect is the builder for selecting fields of UsersCoffeeBean entities.
type UsersCoffeeBeanSelect struct {
	*UsersCoffeeBeanQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (ucbs *UsersCoffeeBeanSelect) Scan(ctx context.Context, v interface{}) error {
	if err := ucbs.prepareQuery(ctx); err != nil {
		return err
	}
	ucbs.sql = ucbs.UsersCoffeeBeanQuery.sqlQuery(ctx)
	return ucbs.sqlScan(ctx, v)
}

func (ucbs *UsersCoffeeBeanSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := ucbs.sql.Query()
	if err := ucbs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
